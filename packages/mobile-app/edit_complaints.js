const fs = require('fs'); let c = fs.readFileSync('src/screens/worker/AssignedComplaints.js', 'utf8'); c = c.replace(chip: { paddingHorizontal: 14, paddingVertical: 8, borderRadius: 50, borderWidth: 1.5, borderColor: T.border, backgroundColor: T.bg }, chip: { flexDirection: 'row', alignItems: 'center', alignSelf: 'flex-start', minHeight: 36, paddingHorizontal: 13, paddingVertical: 7, borderRadius: 18, borderWidth: 1, borderColor: T.border, backgroundColor: '#fff' }).replace(chipTxt: { fontSize: 13, fontWeight: '600', color: T.textL }, chipTxt: { fontSize: 12, fontWeight: '700', color: T.textL }); fs.writeFileSync('src/screens/worker/AssignedComplaints.js', c);
