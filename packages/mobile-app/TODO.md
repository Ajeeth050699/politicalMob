# TODO - Worker View Dashboard full-screen design refresh

- [ ] Step 1: Refactor `WorkerDashboard.js` layout to feel more “full-screen” on mobile
  - adjust hero/header height and scroll spacing
  - tighten/standardize section padding and card sizes
  - improve card hierarchy and tap target spacing
- [ ] Step 2: Update card grids (quick actions, work mix) to use consistent 2-column layout
- [ ] Step 3: Minor polish on `AssignedComplaints.js` card styling to match dashboard look
- [ ] Step 4: Quick sanity check: run mobile app lint/build (or JS lint) if scripts exist
  - (note) lint command should be run from cmd without `&&`/`||` chaining issues
- [ ] Step 5: Verify no broken navigation props and no style syntax issues

